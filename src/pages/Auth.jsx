import React from "react";
import { Link, useNavigate } from "react-router-dom";
export default function Auth({mode}){const navigate=useNavigate();const login=mode==="login",register=mode==="register";return <div className="auth-page"><div className="auth-card card"><Link to="/" className="logo auth-logo">Shop<span>Sphere</span></Link><h1>{login?"Sign in":register?"Create account":"Reset your password"}</h1>{register&&<label>Your name<input placeholder="Full name"/></label>}{!mode.includes("forgot")&&<label>Email<input type="email" placeholder="you@example.com"/></label>}{!mode.includes("forgot")&&<label>Password<input type="password" placeholder="At least 6 characters"/></label>}{mode==="forgot"&&<label>Email<input type="email" placeholder="you@example.com"/></label>}<button className="btn btn-primary full" onClick={()=>navigate("/")}>{mode==="forgot"?"Send reset link":register?"Create account":"Sign in"}</button>{login&&<p><Link to="/forgot-password">Forgot password?</Link></p>}{!register?<p>{login?"New to ShopSphere? ":""}<Link to={login?"/register":"/login"}>{login?"Create your account":"Back to sign in"}</Link></p>:<p>Already have an account? <Link to="/login">Sign in</Link></p>}</div></div>}